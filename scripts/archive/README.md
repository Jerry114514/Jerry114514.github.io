# scripts/archive — 历史抓取脚本

这些脚本原放在仓库根目录，是 HD2_Wiki 数据的早期采集工具，现已归档留存。

⚠ 它们的相对路径假设**在仓库根目录执行**，归档后仅供查阅，直接在此运行找不到目标路径。

仍在使用的脚本：`../fetch_site_data.py`（主站战况数据，由 .github/workflows/fetch-data.yml 每 15 分钟调用，**不要移动**）。
